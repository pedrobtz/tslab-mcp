# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Environments
.venv/
.env

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Server artifacts (runs land in ~/.tslab-mcp by default; this covers a
# repo-local TSLAB_MCP_HOME)
runs/

# Local MCP client config. Generated by `claude mcp add --scope project`, and it
# hardcodes an absolute --directory path, so it is machine-specific. Drop the
# --directory argument to make it portable enough to share.
.mcp.json

# OS
.DS_Store
